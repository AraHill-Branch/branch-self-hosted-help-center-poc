---
aside: false
outline: false
pageClass: api-operation-page
title: "Read Deep Link - Deep Linking API"
description: "Read Deep Link — Deep Linking API endpoint reference: parameters, request and response schema, and copy-paste code samples in 8 languages."
---

# Read Deep Link

<ApiOperation operationId="readDeepLink" />

<div class="api-search-only">

Deep Linking API readDeepLink Read Deep Link This endpoint is used retrieve the deep link url details based on the provided parameter Deep Linking API url query The deep link url against which the details are to be fetched.
 https://example.app.link/abCdEf123  branch_key query The Branch key of the originating app; from your Branch Settings Dashboard
 key_live_xxxx Success. channel Deep link channel feature Deep link feature set campaign Campaig name stage tags Social media tags data ~channel data.~channel Use channel to tag the route that your link reaches users. For example, tag links with 'Facebook' or 'LinkedIn' to help track clicks and installs through those paths separately url data.url Deep link URL $one_time_use data.$one_time_use +url data.+url Deep link URL ~creation_source data.~creation_source $match_duration data.$match_duration $fallback_url data.$fallback_url Change the redirect endpoint for all platforms - so you don't have to enable it by platform. Note that Branch will forward all robots to this URL, which overrides any OG tags entered in the link. System-wide Default URL (set in Link Settings) $identity_id data.$identity_id Deep link identity ID $id data.$id Deep link URL ID $fallback_url_xx data.$fallback_url_xx Change the redirect endpoint for all platforms based on a lower-case Alpha-2 country code. $desktop_url data.$desktop_url Redirect URL for desktop devices - mobile users will default to the app store. $ios_url data.$ios_url Change the redirect endpoint for iOS App Store page for your app (set in Link Settings) $ios_url_xx data.$ios_url_xx Change the redirect endpoint for iOS based on a lower-case Alpha-2 country code. For example, $ios_url_de="..." would redirect Germany deep link clicks. $ipad_url data.$ipad_url Change the redirect endpoint for iPads. Falls back to the $ios_url value when unset. $android_url data.$android_url Change the redirect endpoint for Android Play Store page for your app (set in Link Settings) $android_url_xx data.$android_url_xx Change the redirect endpoint for Android based on a lower-case Alpha-2 country code. For example, $android_url_de="..." would redirect Germany deep link clicks. $samsung_url data.$samsung_url Redirect to Samsung Galaxy Store on Samsung devices. Only link level control. Format should be http://www.samsungapps.com/appquery/appDetail.as?appId={PACKAGE_NAME} $huawei_url data.$huawei_url Redirect to the Huawei App Gallery on Huawei devices. Only link level control. Format should be https://appgallery.huawei.com/app/{HUAWEI_APP_GALLERY_ID} $windows_phone_url data.$windows_phone_url Change the redirect endpoint for Windows OS Windows Phone default URL (set in Link Settings) $blackberry_url data.$blackberry_url Change the redirect endpoint for Blackberry OS BlackBerry default URL (set in Link Settings) Deep link channel $fire_url data.$fire_url Change the redirect endpoint for Amazon Fire OS Fire default URL (set in Link Settings) $ios_wechat_url data.$ios_wechat_url Change the redirect endpoint for WeChat on iOS devices $ios_url value $android_wechat_url data.$android_wechat_url Change the redirect endpoint for WeChat on Android devices $android_url value $web_only data.$web_only Force to open the $fallback_url instead of the app $desktop_web_only data.$desktop_web_only Force to open the $windows_desktop_url, $mac_desktop_url, $desktop_url, or $fallback_url in this order of precedence instead of the app $mobile_web_only data.$mobile_web_only Force to open the$ios_url, $android_url, or $fallback_url in this order of precedence instead of the app $after_click_url data.$after_click_url When a user returns to the browser after going to the app, take them to this URL. iOS only; Android coming soon $afterclick_desktop_url data.$afterclick_desktop_url When a user on desktop returns to the desktop browser after going to the desktop app, take them to this URL. $canonical_url data.$canonical_url This is the url set against the identifier. $deeplink_path data.$deeplink_path The value of the deep link path that you would like us to append to your URI. $og_title data.$og_title Set the title of the Deep Link as it will be seen in social media displays. $og_description data.$og_description Set the description of the Deep Link as it will be seen in social media displays. $og_image_url data.$og_image_url Set the image of the Deep Link as it will be seen in social media displays. alias Instead of our standard encoded short url, you can specify the vanity alias. For example, instead of a random string of characters/integers, you can set the vanity alias as .app.link/devonaustin. type Set type to 1, to make the URL a one-time use URL. It won't deep link after 1 successful deep link. Default is 0, the standard Branch link behavior used by our SDKs. duration In seconds. Only set this key if you want to override the match duration for deep link matching. This is the time that Branch allows a click to remain outstanding and be eligible to be matched with a new app session. This is default set to 7200 (2 hours). Bad request — typically an invalid or missing credential or parameter. error Rate limit exceeded — reduce request frequency and retry. error

</div>
