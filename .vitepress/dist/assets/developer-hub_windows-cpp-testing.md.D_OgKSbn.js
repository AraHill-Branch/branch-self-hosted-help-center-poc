import{_ as a,o as t,c as i,ae as l}from"./chunks/framework.B6gjLfeO.js";const u=JSON.parse('{"title":"Windows Testing","description":"","frontmatter":{"title":"Windows Testing","slug":"windows-cpp-testing"},"headers":[],"relativePath":"developer-hub/windows-cpp-testing.md","filePath":"developer-hub/windows-cpp-testing.md"}'),n={name:"developer-hub/windows-cpp-testing.md"};function o(s,e,r,p,c,h){return t(),i("div",null,[...e[0]||(e[0]=[l(`<h2 id="test-deep-link" tabindex="-1">Test Deep Link <a class="header-anchor" href="#test-deep-link" aria-label="Permalink to &quot;Test Deep Link&quot;">​</a></h2><p>Create a deep link from the <a href="https://dashboard.branch.io/marketing" target="_blank" rel="noreferrer">Branch Dashboard</a>.</p><ol><li>Delete your app from the PC if a production release is installed.</li><li>Compile and test on a PC.</li><li>Paste deep link into an anchor (<code>&lt;a&gt;</code> element) in an HTML page. See the example.html for each TestBed app in the repo.</li><li>Open the HTML page in a browser.</li><li>Click the link in the browser.</li><li>If a dialog appears offering to redirect to your app, click OK.</li><li>The link should launch your app.</li></ol><div class="info custom-block"><p class="custom-block-title">Testing deferred deep linking</p><p>Deferred deep linking is simply deep linking into an app that is not yet installed. Once the app is installed, the context is preserved and the user&#39;s first app-open will have the deep link data from the original Branch link. To test this, uninstall the app from your device, click the Branch link, and manually launch the app from Visual Studio. You should be routed to the correct content within your app.</p></div><h2 id="enable-logging" tabindex="-1">Enable Logging <a class="header-anchor" href="#enable-logging" aria-label="Permalink to &quot;Enable Logging&quot;">​</a></h2><p>The SDK optionally supports logging diagnostic messages to a file. It supports five defined log levels in decreasing order of severity: Error, Warning, Info, Debug and Verbose. The default log level is Error for Release builds and Debug for Debug builds. Debug and Verbose level messages are compiled out in Release builds. Only Error, Warning and Info are available in Release builds. If necessary, you must create the containing directory for the log file.</p><p>Enable log output two ways:</p><ol><li><p>(Simple) Set two environment variables: <code>BRANCHIO_LOG_LEVEL</code> and <code>BRANCHIO_LOG_FILE</code>. Restart your app after setting these environment variables.</p><p><em>[Image: 1758]</em></p></li><li><p>(Custom) Call <code>BranchIO::Log::setLevel</code> and <code>BranchIO::Log::enableFileLogging</code>. Logging is enabled from the time of the call. Call before <code>Branch::create</code> to ensure logging all available messages.</p></li></ol><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;BranchIO/Util/Log.h&gt;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>// Debug and Verbose levels are compiled out in Release builds.</span></span>
<span class="line"><span>BranchIO::Log::setLevel(Log::Verbose);</span></span>
<span class="line"><span></span></span>
<span class="line"><span>// This ia a UTF-8 string, not UTF-16.</span></span>
<span class="line"><span>// Be sure to escape backslashes in file paths.</span></span>
<span class="line"><span>BranchIO::Log::enableFileLogging(&quot;C:\\\\path\\\\to\\\\logfile.log&quot;);</span></span></code></pre></div><p>See the <code>setupSDKLogging</code> method in <code>BranchOperations.cpp</code> in the TestBed apps in the repo for a full working example of SDK logging to <code>%LocalAppData%</code>.</p><h2 id="use-test-key" tabindex="-1">Use Test Key <a class="header-anchor" href="#use-test-key" aria-label="Permalink to &quot;Use Test Key&quot;">​</a></h2><ol><li>Use the Branch <code>test key</code> instead of the <code>live key</code></li><li>Use in <code>Branch::create</code> <a href="./windows-cpp-testing.html#initialize-branch">Initialize Branch</a></li><li>The Branch key of your app must match the Branch key of your deep link</li><li>Remove before releasing to production</li></ol><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>auto* branch = BranchIO::Branch::create(L&quot;key_test_xxxxxxxx&quot;, &amp;appInfo);</span></span></code></pre></div><p>The Branch key is passed from the app to the Branch SDK as the first parameter when calling <code>create</code>. You may pass any valid Branch key. For example, the TestBed apps in the repo pass the key using a command-line build parameter, <code>/D &quot;BRANCH_KEY=L\\&quot;key_live_xxxxxxxx\\&quot;&quot;</code>. This preprocessor definition is then available to pass to the <code>Branch::create</code> call. This approach allows you to specify a different key per build configuration, e.g. test key for Debug and live key for Release.</p><div class="info custom-block"><p class="custom-block-title">Note</p><p>If you use a different URI scheme for your app&#39;s test key in the Dashboard, you must also adjust the URI scheme you use in the Registry or your MSIX package. See the Basic Integration guide for more details.</p></div><h2 id="sample-apps" tabindex="-1">Sample Apps <a class="header-anchor" href="#sample-apps" aria-label="Permalink to &quot;Sample Apps&quot;">​</a></h2><p>There are four variants in the <a href="https://github.com/BranchMetrics/cpp-branch-deep-linking-attribution/tree/master/BranchSDK-Samples" target="_blank" rel="noreferrer">GitHub repo</a> of an example application called TestBed. In all cases except TestBed-Conan, you must first install the <a href="https://github.com/BranchMetrics/cpp-branch-deep-linking-attribution/releases/latest" target="_blank" rel="noreferrer">BranchSDK.msi</a>. Each app has a separate Branch key and domain so that they can all be installed side-by-side on the same PC. Each app has a separate example.html page with a link on that domain to launch the associated app. All apps may be uninstalled along with all Registry configuration via Add or Remove Programs in Windows Settings. All TestBed variants require the Windows 10 SDK version 10.0.18363.0 (1909), available through the Visual Studio Installer.</p><p>All TestBed variants leave SDK log files in <code>%LocalAppData%\\Branch\\TestBed</code>. Debug builds will generate Verbose-level logs. Release builds generate Info-level logs.</p><div class="info custom-block"><p class="custom-block-title">Note</p><p>It may be necessary to enable <a href="https://docs.microsoft.com/en-us/windows/apps/get-started/enable-your-device-for-development" target="_blank" rel="noreferrer">Developer Mode</a> on a Windows device in order to deploy TestBed to the Start menu.</p></div><h3 id="_1-testbed" tabindex="-1">1. <a href="https://github.com/BranchMetrics/cpp-branch-deep-linking-attribution/tree/master/BranchSDK-Samples/TestBed" target="_blank" rel="noreferrer">TestBed</a> <a class="header-anchor" href="#_1-testbed" aria-label="Permalink to &quot;1. [TestBed](https://github.com/BranchMetrics/cpp-branch-deep-linking-attribution/tree/master/BranchSDK-Samples/TestBed)&quot;">​</a></h3><p>This app is built against the Branch SDK and all dependencies from the installed MSI. It uses MSIX to package the Win32 app for deployment with a URI protocol to launch the app.</p><p>To build and test:</p><ul><li>Ensure you have the UWP workload installed for Visual Studio (necessary for MSIX).</li><li>Install the MSI.</li><li>Open the TestBed.sln.</li><li>Select a build configuration (Debug or Release) and an architecture (x86 or x64) and then Build &gt; Deploy Solution.</li><li>The app will appear in the Start menu.</li><li>The app is not installed in a system location. The binary is located in your local build folder in the repo.</li><li>Click the link in example.html to open a Branch link in the app.</li></ul><h3 id="_2-testbed-local" tabindex="-1">2. <a href="https://github.com/BranchMetrics/cpp-branch-deep-linking-attribution/tree/master/BranchSDK-Samples/TestBed-Local" target="_blank" rel="noreferrer">TestBed-Local</a> <a class="header-anchor" href="#_2-testbed-local" aria-label="Permalink to &quot;2. [TestBed-Local](https://github.com/BranchMetrics/cpp-branch-deep-linking-attribution/tree/master/BranchSDK-Samples/TestBed-Local)&quot;">​</a></h3><p>This app is built against source code from the local repo, taking only Poco and other dependencies from the installed MSI. It uses MSIX to package the Win32 app for deployment with a URI protocol to launch the app.</p><p>To build and test:</p><ul><li>Ensure you have the UWP workload installed for Visual Studio (necessary for MSIX).</li><li>Install the MSI.</li><li>Open the TestBed-Local.sln.</li><li>Select a build configuration (Debug or Release) and an architecture (x86 or x64) and then Build &gt; Deploy Solution.</li><li>The app will appear in the Start menu.</li><li>The app is not installed in a system location. The binary is located in your local build folder in the repo.</li><li>Click the link in example.html to open a Branch link in the app.</li></ul><h3 id="_3-testbed-basic" tabindex="-1">3. <a href="https://github.com/BranchMetrics/cpp-branch-deep-linking-attribution/tree/master/BranchSDK-Samples/TestBed-Basic" target="_blank" rel="noreferrer">TestBed-Basic</a> <a class="header-anchor" href="#_3-testbed-basic" aria-label="Permalink to &quot;3. [TestBed-Basic](https://github.com/BranchMetrics/cpp-branch-deep-linking-attribution/tree/master/BranchSDK-Samples/TestBed-Basic)&quot;">​</a></h3><p>This app is built against source code from the local repo, taking only Poco and other dependencies from the installed MSI. It uses the Wix toolset to package the Win32 app for deployment as an MSI with custom Registry entries to launch the app from a URI. This version of TestBed does not use MSIX or require the UWP workload.</p><p>To build and test:</p><ul><li>Install the MSI.</li><li>Install the Wix Toolset from <a href="https://github.com/wixtoolset/wix3/releases" target="_blank" rel="noreferrer">https://github.com/wixtoolset/wix3/releases</a>.</li><li>Install the Wix Visual Studio Extension (Extensions &gt; Manage Extensions).</li><li>Open the TestBed-Basic.sln.</li><li>Select Debug build configuration (required for the Wix installer project).</li><li>Select x86 architecture (required for the Wix installer project).</li><li>Select Build &gt; Build Solution</li><li>The app will not appear in the Start menu.</li><li>The app is installed in C:\\Program Files (x86)\\Branch TestBed (Basic).</li><li>Click the link in Welcome.html to open a Branch link in the app.</li></ul><div class="info custom-block"><p class="custom-block-title">Note</p><p>The Wix Toolset requires .NET Framework 3.5.1. Enable it under Windows Features to install the Wix Toolset.</p></div><p><em>[Image: 415]</em></p><h3 id="" tabindex="-1"><a class="header-anchor" href="#" aria-label="Permalink to &quot;&quot;">​</a></h3>`,34)])])}const g=a(n,[["render",o]]);export{u as __pageData,g as default};
