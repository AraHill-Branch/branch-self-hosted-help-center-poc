import{_ as e,o as n,c as t,ae as s}from"./chunks/framework.B6gjLfeO.js";const h=JSON.parse('{"title":"Windows Advanced Features","description":"","frontmatter":{"title":"Windows Advanced Features","slug":"windows-cpp-advanced-features"},"headers":[],"relativePath":"developer-hub/windows-cpp-advanced-features.md","filePath":"developer-hub/windows-cpp-advanced-features.md"}'),o={name:"developer-hub/windows-cpp-advanced-features.md"};function i(r,a,p,l,c,d){return n(),t("div",null,[...a[0]||(a[0]=[s(`<h2 id="create-a-branch-instance" tabindex="-1">Create a Branch Instance <a class="header-anchor" href="#create-a-branch-instance" aria-label="Permalink to &quot;Create a Branch Instance&quot;">​</a></h2><p>Creating a Branch Instance takes a Branch Key, and Application Information.<br> Initialize the AppInfo with settings specific to your app.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>// Set AppInfo properties as necessary</span></span>
<span class="line"><span>BranchIO::AppInfo _appInfo;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>_branchInstance = BranchIO::create(&quot;key_live_xxx&quot;, &amp;_appInfo);</span></span></code></pre></div><h2 id="create-a-branch-session" tabindex="-1">Create a Branch Session <a class="header-anchor" href="#create-a-branch-session" aria-label="Permalink to &quot;Create a Branch Session&quot;">​</a></h2><p>Creating a Branch Session takes an optional link parameter and a callback.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>_branchInstance-&gt;openSession(&quot;&quot;, myCallback);</span></span></code></pre></div><h2 id="persistent-identities" tabindex="-1">Persistent identities <a class="header-anchor" href="#persistent-identities" aria-label="Permalink to &quot;Persistent identities&quot;">​</a></h2><p>Often, you might have your own user IDs, or want referral and event data to persist across platforms or uninstall/reinstall. It&#39;s helpful if you know the users access the service from different devices. This where we introduce the concept of an &#39;identity&#39;.</p><p>To identify a user, just call:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>_branchInstance-&gt;setIdentity(&quot;user id&quot;, myCallback);</span></span></code></pre></div><h2 id="logout" tabindex="-1">Logout <a class="header-anchor" href="#logout" aria-label="Permalink to &quot;Logout&quot;">​</a></h2><p>If you provide a logout function in the app, be sure to clear the user when the logout completes. This will ensure that all the stored parameters get cleared and all events are properly attributed to the right identity.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>_branchInstance-&gt;logout(myCallback):</span></span></code></pre></div><div class="info custom-block"><p class="custom-block-title">Note</p><p>Neither <code>setIdentity</code> nor <code>logout</code> can be called before <code>openSession</code>. This will generate an error and have no other effect. Once set or removed, the identity or lack of one is persistent.</p></div><h2 id="tracking-user-actions-and-events" tabindex="-1">Tracking User Actions and Events <a class="header-anchor" href="#tracking-user-actions-and-events" aria-label="Permalink to &quot;Tracking User Actions and Events&quot;">​</a></h2><h3 id="standard-events" tabindex="-1">Standard Events <a class="header-anchor" href="#standard-events" aria-label="Permalink to &quot;Standard Events&quot;">​</a></h3><p>Use the BranchIO::StandardEvent class to track special user actions or application-specific events beyond app installs, opens, and sharing. You can track events such as when a user adds an item to an on-line shopping cart, or searches for a keyword etc. StandardEvent provides an interface to add content(s) in order to associate content(s) with events. You can view analytics for the StandardEvents you fire on the Branch dashboard.</p><p><em>BranchIO::StandardEvent</em> enumerate the most commonly tracked events and event parameters that can be used with StandardEvent for the best results. You can always use custom event names and event parameters.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;BranchIO/Event/StandardEvent.h&gt;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>using namespace BranchIO;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>StandardEvent event(StandardEvent::Type::PURCHASE);</span></span>
<span class="line"><span>event.setCoupon(&quot;TestCoupon&quot;)</span></span>
<span class="line"><span>     .setCurrency(&quot;USD&quot;)</span></span>
<span class="line"><span>     .setDescription(&quot;Test Description&quot;)</span></span>
<span class="line"><span>     .setRevenue(99.99)</span></span>
<span class="line"><span>     .setSearchQuery(&quot;Some Search Query&quot;)</span></span>
<span class="line"><span>     .setShipping(1.99)</span></span>
<span class="line"><span>     .setTax(.99)</span></span>
<span class="line"><span>     .setTransactionId(&quot;Transaction123&quot;);</span></span>
<span class="line"><span></span></span>
<span class="line"><span>_branchInstance-&gt;sendEvent(event, new StandardEventCallback);</span></span></code></pre></div><h3 id="custom-events" tabindex="-1">Custom Events <a class="header-anchor" href="#custom-events" aria-label="Permalink to &quot;Custom Events&quot;">​</a></h3><p>Use the BranchIO::CustomEvent class to track special user actions or application-specific events beyond standard Branch events. Custom Events also appear in Branch Dashboard analytics.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;BranchIO/Event/CustomEvent.h&gt;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>using namespace BranchIO;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>CustomEvent event(L&quot;MyCustomEvent&quot;);</span></span>
<span class="line"><span>event.setAdType(Event::AdType::BANNER);</span></span>
<span class="line"><span>event.addCustomDataProperty(L&quot;foo&quot;, L&quot;Bar&quot;);</span></span>
<span class="line"><span></span></span>
<span class="line"><span>_branchInstance-&gt;sendEvent(event, new CustomEventCallback);</span></span></code></pre></div><h2 id="enable-or-disable-user-tracking" tabindex="-1">Enable or Disable User Tracking <a class="header-anchor" href="#enable-or-disable-user-tracking" aria-label="Permalink to &quot;Enable or Disable User Tracking&quot;">​</a></h2><p>If you need to comply with a user&#39;s request to not be tracked for GDPR purposes, or otherwise determine that a user should not be tracked, utilize this field to prevent Branch from sending network requests. This setting can also be enabled across all users for a particular link, or across your Branch Links.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>_branchInstance-&gt;getAdvertiserInfo().disableTracking();</span></span></code></pre></div><p>To enable tracking again once it&#39;s been disabled, call <code>enableTracking</code> instead of <code>disableTracking</code>.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>_branchInstance-&gt;getAdvertiserInfo().enableTracking();</span></span>
<span class="line"><span>/*</span></span>
<span class="line"><span> * When reenabling tracking, it&#39;s necessary to open a new session afterward.</span></span>
<span class="line"><span> */</span></span>
<span class="line"><span>_branchInstance-&gt;openSession(&quot;&quot;, myCallback);</span></span></code></pre></div><div class="info custom-block"><p class="custom-block-title">Note</p><p>When reenabling tracking, a new session must be created using <code>Branch::openSession</code> before any further SDK calls may be made.</p></div><p>To default to tracking disabled on first launch, add a <code>REG_DWORD</code> registry value <code>trackingDisabled</code> with value 1 to the key <code>HKEY_CURRENT_USER\\SOFTWARE\\BranchIO\\</code>. This prevents deferred deep linking or install attribution. After SDK initialization, call <code>disableTracking()</code> to reenable.</p><h2 id="generating-branch-links" tabindex="-1">Generating Branch Links <a class="header-anchor" href="#generating-branch-links" aria-label="Permalink to &quot;Generating Branch Links&quot;">​</a></h2><p>Branch Links can be created in-app. When they are, and <code>setIdentity</code> has been called to associate a User ID with the current user session, Branch Links will be associated with that User ID.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>// you can access this data from any instance that installs or opens the app from this link</span></span>
<span class="line"><span></span></span>
<span class="line"><span>BranchIO::LinkInfo linkInfo;</span></span>
<span class="line"><span>linkInfo</span></span>
<span class="line"><span>    .addTag(&quot;Tag1&quot;)</span></span>
<span class="line"><span>    .setChannel(&quot;Sharing_Channel_name&quot;)</span></span>
<span class="line"><span>    .setFeature(&quot;my_feature_name&quot;)</span></span>
<span class="line"><span>    .addControlParameter(&quot;$android_deeplink_path&quot;, &quot;custom/path/*&quot;)</span></span>
<span class="line"><span>    .addControlParameter(&quot;$ios_url&quot;, &quot;http://example.com/ios&quot;)</span></span>
<span class="line"><span>    .setDuration(100);</span></span>
<span class="line"><span></span></span>
<span class="line"><span>// Add some additional control parameters</span></span>
<span class="line"><span>linkInfo</span></span>
<span class="line"><span>    .addControlParameter(&quot;$canonical_identifier&quot;, &quot;item/12345&quot;)</span></span>
<span class="line"><span>    .addControlParameter(&quot;$canonical_url&quot;, https://branch.io/deepviews&quot;)</span></span>
<span class="line"><span>    .addControlParameter(&quot;$og_title&quot;, &quot;My Content Title&quot;)</span></span>
<span class="line"><span>    .addControlParameter(&quot;$og_description&quot;, &quot;my_product_description1&quot;)</span></span>
<span class="line"><span>    .addControlParameter(&quot;$og_image_url&quot;, &quot;https://example.com/mycontent-12345.png&quot;);</span></span>
<span class="line"><span></span></span>
<span class="line"><span>// Generate a long branch link, synchronously.</span></span>
<span class="line"><span>string url = linkInfo.createLongUrl(_branchInstance);</span></span>
<span class="line"><span></span></span>
<span class="line"><span>// Create a short branch link, asynchronously (requires a server round trip).</span></span>
<span class="line"><span>// Falls back to a long URL in case of request failure.</span></span>
<span class="line"><span>linkInfo.createUrl(_branchInstance, _myCallback);</span></span></code></pre></div><div class="info custom-block"><p class="custom-block-title">Notes</p><ul><li>The <code>LinkInfo</code> destructor blocks until the asynchronous URL request completes. Use <code>LinkInfo::cancel()</code> to force immediate termination of the asynchronous request.</li><li>You can customize the Facebook OG tags of each URL if you want to dynamically share content by using the following<br><em>optional keys in the data dictionary</em>. Please use this<br><a href="https://developers.facebook.com/tools/debug/" target="_blank" rel="noreferrer">Facebook tool</a> to debug the OG tags.</li></ul></div><table tabindex="0"><thead><tr><th>Key</th><th>Value</th></tr></thead><tbody><tr><td>&quot;$og_title&quot;</td><td>The title you&#39;d like to appear for the link in social media.</td></tr><tr><td>&quot;$og_description&quot;</td><td>The description you&#39;d like to appear for the link in social media.</td></tr><tr><td>&quot;$og_image_url&quot;</td><td>The URL for the image you&#39;d like to appear for the link in social media.</td></tr><tr><td>&quot;$og_video&quot;</td><td>The URL for the video.</td></tr><tr><td>&quot;$og_url&quot;</td><td>The URL you&#39;d like to appear.</td></tr><tr><td>&quot;$og_app_id&quot;</td><td>The OG app ID. Optional and rarely used.</td></tr></tbody></table><p>Also, you do custom redirection by inserting the following <em>optional keys in the dictionary</em>:</p><table tabindex="0"><thead><tr><th>Key</th><th>Value</th></tr></thead><tbody><tr><td>&quot;$desktop_url&quot;</td><td>Where to send the user on a desktop or laptop. By default it is the Branch-hosted text-me service.</td></tr><tr><td>&quot;$android_url&quot;</td><td>The replacement URL for the Play Store to send the user if they don&#39;t have the app. <em>Only necessary if you want a mobile web splash</em></td></tr><tr><td>&quot;$ios_url&quot;</td><td>The replacement URL for the App Store to send the user if they don&#39;t have the app. <em>Only necessary if you want a mobile web splash</em></td></tr><tr><td>&quot;$ipad_url&quot;</td><td>Same as above but for iPad Store.</td></tr><tr><td>&quot;$fire_url&quot;</td><td>Same as above but for Amazon Fire Store.</td></tr><tr><td>&quot;$blackberry_url&quot;</td><td>Same as above but for Blackberry Store.</td></tr><tr><td>&quot;$windows_phone_url&quot;</td><td>Same as above but for Windows Store.</td></tr></tbody></table><p>You have the ability to control the direct deep linking of each link by inserting the following <em>optional keys in the dictionary</em>:</p><table tabindex="0"><thead><tr><th>Key</th><th>Value</th></tr></thead><tbody><tr><td>&quot;$deeplink_path&quot;</td><td>The value of the deep link path that you&#39;d like us to append to the URI. For example, you could specify &quot;$deeplink_path&quot;: &quot;radio/station/456&quot; and we&#39;ll open the app with the URI &quot;theapp://radio/station/456?link_click_id=branch-identifier&quot;. This is primarily for supporting legacy deep linking infrastructure.</td></tr><tr><td>&quot;$always_deeplink&quot;</td><td><strong>true</strong> or <strong>false</strong>. (default is not to deep link first) This key can be specified to have our linking service force try to open the app, even if we&#39;re not sure the user has the app installed. If the app is not installed, we fall back to the respective app store or $platform_url key. By default, we only open the app if we&#39;ve seen a user initiate a session in the app from a Branch Link (has been cookied and deep linked by Branch).</td></tr></tbody></table>`,38)])])}const b=e(o,[["render",i]]);export{h as __pageData,b as default};
