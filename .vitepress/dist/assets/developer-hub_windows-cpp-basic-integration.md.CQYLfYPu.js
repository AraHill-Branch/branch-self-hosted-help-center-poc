import{_ as a,o as n,c as s,ae as t}from"./chunks/framework.B6gjLfeO.js";const u=JSON.parse('{"title":"Windows Basic Integration","description":"","frontmatter":{"title":"Windows Basic Integration","slug":"windows-cpp-basic-integration"},"headers":[],"relativePath":"developer-hub/windows-cpp-basic-integration.md","filePath":"developer-hub/windows-cpp-basic-integration.md"}'),i={name:"developer-hub/windows-cpp-basic-integration.md"};function o(l,e,p,r,c,d){return n(),s("div",null,[...e[0]||(e[0]=[t(`<div class="info custom-block"><p class="custom-block-title">UWP &amp; .NET</p><p>The Branch Windows SDK is not compatible with Universal Windows Platform (UWP) or .NET. Support for UWP and .NET applications is not currently available.</p></div><div class="tip custom-block"><p class="custom-block-title">SDK Stats</p><p><strong>Open Source Github Repo</strong>:<br><a href="https://github.com/BranchMetrics/cpp-branch-deep-linking-attribution" target="_blank" rel="noreferrer">https://github.com/BranchMetrics/cpp-branch-deep-linking-attribution</a></p></div><div class="warning custom-block"><p class="custom-block-title">Prerequisite</p><p>Before you implement the SDK, please ensure you have <a href="./configure-default-link-behaviors.html#7-desktop-app-default-link-behavior">Configured Link Behaviors for Redirects and Link Domains</a>.</p></div><h2 id="visual-studio-configuration" tabindex="-1">Visual Studio Configuration <a class="header-anchor" href="#visual-studio-configuration" aria-label="Permalink to &quot;Visual Studio Configuration&quot;">​</a></h2><h3 id="visual-studio-2022" tabindex="-1">Visual Studio 2022 <a class="header-anchor" href="#visual-studio-2022" aria-label="Permalink to &quot;Visual Studio 2022&quot;">​</a></h3><ul><li>From within the Visual Studio Installer, install the <strong>Universal Windows Platform development</strong> workload.</li><li>In <strong>Installation Details</strong> -&gt; <strong>Universal Windows Platform development,</strong> check the <strong>C++ (v14x) Universal Windows Platform tools</strong> option(s).</li></ul><p><em>[Image: 2620]</em></p><h3 id="visual-studio-2019" tabindex="-1">Visual Studio 2019 <a class="header-anchor" href="#visual-studio-2019" aria-label="Permalink to &quot;Visual Studio 2019&quot;">​</a></h3><p>Branch SDK uses C++/WinRT APIs. Visual Studio 2019 doesn’t include these APIs by default. So if you are using Visual Studio 2019 for developing your app, install <strong>Microsoft.Windows.CppWinRT</strong> NuGet package.</p><p><strong>To install</strong></p><ul><li>Click <strong>Project</strong> &gt; <strong>Manage NuGet Packages...</strong> &gt; <strong>Browse</strong>.</li><li>Type or paste <strong>Microsoft.Windows.CppWinRT</strong> in the search box.</li><li>Select the item in search results, and then click <strong>Install</strong> to install the package for that project.</li></ul><p><em>[Image: 2086]</em></p><h2 id="_1-download-install-the-branch-sdk" tabindex="-1">1. Download &amp; Install the Branch SDK <a class="header-anchor" href="#_1-download-install-the-branch-sdk" aria-label="Permalink to &quot;1. Download &amp; Install the Branch SDK&quot;">​</a></h2><div class="info custom-block"><p class="custom-block-title">Note</p><p>Visual Studio 2019+ is required (Earlier versions are not supported.)</p></div><p><strong>Download the SDK</strong></p><p>The Branch SDK Installer is available for download <a href="https://github.com/BranchMetrics/cpp-branch-deep-linking-attribution/releases/latest" target="_blank" rel="noreferrer">here</a>. This downloads a file called <code>BranchSDK.msi</code>.</p><div class="info custom-block"><p class="custom-block-title">Note</p><p>If you have installed any release of the MSI, you should manually uninstall before installing the new version.</p></div><p><strong>Install the SDK</strong></p><p>Double-click the <code>BranchSDK.msi</code> file to begin installation.</p><p><em>[Image: 988]</em></p><p>Once the installation finishes, all headers and libraries will be installed under <code>C:\\Program Files (x86)\\Branch SDK</code>.</p><div class="info custom-block"><p class="custom-block-title">Anti-malware Alert</p><p>The MSI may be flagged by malware blockers in some cases. For example, with Windows Defender, it may be necessary to click the &quot;More info&quot; link first to allow installation to proceed via the &quot;Run anyway&quot; button. Please be sure only to install the MSI from <a href="https://github.com/BranchMetrics/cpp-branch-deep-linking-attribution/releases" target="_blank" rel="noreferrer">https://github.com/BranchMetrics/cpp-branch-deep-linking-attribution/releases</a>.</p></div><h2 id="_2-build-against-the-installed-branch-sdk" tabindex="-1">2. Build Against the Installed Branch SDK <a class="header-anchor" href="#_2-build-against-the-installed-branch-sdk" aria-label="Permalink to &quot;2. Build Against the Installed Branch SDK&quot;">​</a></h2><p>The SDK will be installed in <code>C:\\Program Files (x86)\\Branch SDK</code>.</p><p>Open your project&#39;s Properties in Visual Studio.</p><h4 id="_2-1-update-c-version" tabindex="-1">2.1 Update C++ Version <a class="header-anchor" href="#_2-1-update-c-version" aria-label="Permalink to &quot;2.1 Update C++ Version&quot;">​</a></h4><p>Branch SDK uses features from the C++17 standard, set project property C/C++ &gt; Language &gt; C++ Language Standard &gt; ISO C++17 Standard (/std:c++17) in Visual Studio.</p><p><em>[Image: 1574]</em></p><h4 id="_2-2-add-include-path" tabindex="-1">2.2 Add include path <a class="header-anchor" href="#_2-2-add-include-path" aria-label="Permalink to &quot;2.2 Add include path&quot;">​</a></h4><p>Under <code>C/C++ &gt; General</code>, add <code>C:\\Program Files (x86)\\Branch SDK\\include</code> to <code>Additional Include Directories</code> for All Configurations and All Platforms. The corresponding command-line option is <code>/I&quot;C:\\Program Files (x86)\\Branch SDK\\include&quot;</code>.</p><p><em>[Image: 1792]**[Image: 1792]</em></p><h4 id="_2-3-add-preprocessor-definitions" tabindex="-1">2.3 Add preprocessor definitions <a class="header-anchor" href="#_2-3-add-preprocessor-definitions" aria-label="Permalink to &quot;2.3 Add preprocessor definitions&quot;">​</a></h4><p>Under <code>C/C++ &gt; Preprocessor</code>, define <code>DEBUG</code> in Debug builds. All screenshots are for Debug x64 where configurations differ.</p><p><em>[Image: 1332]</em></p><h4 id="_2-4-verify-runtime-library" tabindex="-1">2.4 Verify Runtime Library <a class="header-anchor" href="#_2-4-verify-runtime-library" aria-label="Permalink to &quot;2.4 Verify Runtime Library&quot;">​</a></h4><p>We support <code>MD</code>, <code>MDd</code>, <code>MT</code> and <code>MTd</code> runtimes. Under <code>C/C++ &gt; Code Generation</code>, select the appropriate option.</p><p><em>[Image: 1792]</em></p><h4 id="_2-5-add-library-paths" tabindex="-1">2.5 Add library paths <a class="header-anchor" href="#_2-5-add-library-paths" aria-label="Permalink to &quot;2.5 Add library paths&quot;">​</a></h4><p>Under <code>Linker &gt; General</code>, add the library directory for each combination of configuration and platform to <code>Additional Library Search Paths</code>:</p><table tabindex="0"><thead><tr><th>Platform</th><th>Additional Library Search Paths</th></tr></thead><tbody><tr><td>Debug MDd</td><td><code>C:\\Program Files (x86)\\Branch SDK\\lib\\MDd_64</code></td></tr><tr><td>Release MD</td><td><code>C:\\Program Files (x86)\\Branch SDK\\lib\\MD_64</code></td></tr><tr><td>Debug MTd</td><td><code>C:\\Program Files (x86)\\Branch SDK\\lib\\MTd_64</code></td></tr><tr><td>Release MT</td><td><code>C:\\Program Files (x86)\\Branch SDK\\lib\\MT_64</code></td></tr></tbody></table><p>The command-line option for MDd, e.g., is <code>/LIBPATH:&quot;C:\\Program Files (x86)\\Branch SDK\\lib\\MDd_64&quot;</code>.</p><p>Screenshots show an MT example.</p><p><em>[Image: 1022]**[Image: 1036]</em></p><h4 id="_2-6-add-libraries" tabindex="-1">2.6 Add libraries <a class="header-anchor" href="#_2-6-add-libraries" aria-label="Permalink to &quot;2.6 Add libraries&quot;">​</a></h4><p>Under <code>Linker &gt; Input</code>, add the following (in order) to <code>Additional Dependencies</code>, depending on the configuration:</p><div class="vp-code-group vp-adaptive-theme"><div class="tabs"><input type="radio" name="group-A3Gzh" id="tab-uCAd3D2" checked><label data-title="MDd" for="tab-uCAd3D2">MDd</label><input type="radio" name="group-A3Gzh" id="tab-GnobIcB"><label data-title="MTd" for="tab-GnobIcB">MTd</label><input type="radio" name="group-A3Gzh" id="tab-zx9GbKZ"><label data-title="MD" for="tab-zx9GbKZ">MD</label><input type="radio" name="group-A3Gzh" id="tab-Voa9Mqn"><label data-title="MT" for="tab-Voa9Mqn">MT</label></div><div class="blocks"><div class="language-plaintext vp-adaptive-theme active"><button title="Copy Code" class="copy"></button><span class="lang">plaintext</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BranchIOmdd.lib</span></span>
<span class="line"><span>                    Iphlpapi.lib</span></span>
<span class="line"><span>                    crypt32.lib</span></span>
<span class="line"><span>                    ws2_32.lib</span></span></code></pre></div><div class="language-plaintext vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">plaintext</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BranchIOmtd.lib</span></span>
<span class="line"><span>                    Iphlpapi.lib</span></span>
<span class="line"><span>                    crypt32.lib</span></span>
<span class="line"><span>                    ws2_32.lib</span></span></code></pre></div><div class="language-plaintext vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">plaintext</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BranchIOmd.lib</span></span>
<span class="line"><span>                    Iphlpapi.lib</span></span>
<span class="line"><span>                    crypt32.lib</span></span>
<span class="line"><span>                    ws2_32.lib</span></span></code></pre></div><div class="language-plaintext vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">plaintext</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BranchIOmt.lib</span></span>
<span class="line"><span>                    Iphlpapi.lib</span></span>
<span class="line"><span>                    crypt32.lib</span></span>
<span class="line"><span>                    ws2_32.lib</span></span></code></pre></div></div></div><p><em>[Image: 1574]**[Image: 1332]</em></p><p>Your app should now build and run. See the TestBed apps for reference.</p><h2 id="_3-set-up-uri-redirection" tabindex="-1">3. Set up URI Redirection <a class="header-anchor" href="#_3-set-up-uri-redirection" aria-label="Permalink to &quot;3. Set up URI Redirection&quot;">​</a></h2><p>For Win32 apps, Branch uses URI-based redirection. When a user opens a link to a Win32 app, Branch responds with a URI redirect using the custom URI scheme you configured in Branch:</p><ul><li><strong>New Branch</strong>: In the left-hand navigation, go to <strong>Configuration</strong> and select <strong>Link Controls</strong>. Navigate to the <strong>Link Routing Rules</strong> tab, then find the <strong>Desktop Routing</strong> section to configure your Desktop Redirects.</li><li><strong>Legacy Branch</strong>: Navigate to the <a href="https://branch.dashboard.branch.io/configuration/general" target="_blank" rel="noreferrer">Configuration page</a> and find the Desktop Redirects section.</li></ul><p>In order to launch the app from a Branch link, it is necessary to make certain entries in the Registry to open the app from a custom URI scheme.</p><h4 id="_3-1-via-custom-registry-entries" tabindex="-1">3.1 via Custom Registry Entries <a class="header-anchor" href="#_3-1-via-custom-registry-entries" aria-label="Permalink to &quot;3.1 via Custom Registry Entries&quot;">​</a></h4><p>When not using MSIX, it is necessary to make custom Registry entries to associate your URI scheme with your app. If your URI scheme is <code>myurischeme</code>, these entries should be made under <code>HKEY_CURRENT_USER\\SOFTWARE\\Classes\\myurischeme</code>. If your app supports system-wide installation, you may optionally us <code>HKEY_CLASSES_ROOT\\myurischeme</code>.</p><p>It is recommended to add this Registry key as part of your app&#39;s installation. For example, using the Wix packager with the WixUI_InstallDir template:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;!-- Add Registry entries for URI redirection --&gt;</span></span>
<span class="line"><span>        &lt;!-- testbedbasic is the URI scheme from the Branch Dashboard --&gt;</span></span>
<span class="line"><span>        &lt;RegistryKey Root=&quot;HKCU&quot; Key=&quot;SOFTWARE\\Classes\\myurischeme&quot; &gt;</span></span>
<span class="line"><span>        &lt;RegistryValue Value=&quot;My Application&quot; Type=&quot;string&quot; KeyPath=&quot;yes&quot; /&gt;</span></span>
<span class="line"><span>        &lt;RegistryValue Name=&quot;URL Protocol&quot; Value=&quot;&quot; Type=&quot;string&quot; /&gt;</span></span>
<span class="line"><span>        &lt;RegistryKey Key=&quot;DefaultIcon&quot;&gt;</span></span>
<span class="line"><span>        &lt;RegistryValue Value=&quot;[INSTALLBINFOLDER]MyApp.exe,1&quot; Type=&quot;string&quot;/&gt;</span></span>
<span class="line"><span>        &lt;/RegistryKey&gt;</span></span>
<span class="line"><span>        &lt;RegistryKey Key=&quot;shell\\open\\command&quot;&gt;</span></span>
<span class="line"><span>        &lt;RegistryValue Value=&#39;&quot;[INSTALLBINFOLDER]MyApp.exe&quot; &quot;%1&quot;&#39; Type=&quot;string&quot;/&gt;</span></span>
<span class="line"><span>        &lt;/RegistryKey&gt;</span></span>
<span class="line"><span>        &lt;/RegistryKey&gt;</span></span></code></pre></div><p>The <code>(Default)</code> value of the base registry key (<code>HKCU\\SOFTWARE\\Classes\\myurischeme</code>) is a descriptive label. In many cases, it is <code>URL:myurischeme</code> by default.</p><p>The <code>shell\\open\\command</code> key passes the URI to the app as the command-line argument (<code>lpCmdLine</code>). You may also opt to pass the URI in any other way, e.g. using a command-line argument if your apps supports it.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;RegistryKey Key=&quot;shell\\open\\command&quot;&gt;</span></span>
<span class="line"><span>        &lt;RegistryValue</span></span>
<span class="line"><span>        Value=&#39;&quot;[INSTALLBINFOLDER]MyApp.exe&quot; &quot;/uri:%1&quot;&#39;</span></span>
<span class="line"><span>        Type=&quot;string&quot;/&gt;</span></span>
<span class="line"><span>        &lt;/RegistryKey&gt;</span></span></code></pre></div><div class="info custom-block"><p class="custom-block-title">Notes</p><ol><li>The command-line argument may be received with surrounding quotes that need to be stripped.</li><li>When using MSIX, the package automatically sets up a similar Registry structure on installation.</li></ol></div><p>See the <a href="https://github.com/BranchMetrics/cpp-branch-deep-linking-attribution/tree/master/BranchSDK-Samples/TestBed-Basic" target="_blank" rel="noreferrer">TestBed-Basic</a> app in the repo for an example using Wix to create the required Registry keys. These Registry entries cause URIs with the <code>testbedbasic</code> scheme to open the TestBed-Basic app.</p><p><em>[Image: 988]<strong>[Image: 988]</strong>[Image: 988]</em></p><h2 id="_4-initialize-branch" tabindex="-1">4. Initialize Branch <a class="header-anchor" href="#_4-initialize-branch" aria-label="Permalink to &quot;4. Initialize Branch&quot;">​</a></h2><h4 id="_4-1-initialize-the-branch-sdk" tabindex="-1">4.1 Initialize the Branch SDK <a class="header-anchor" href="#_4-1-initialize-the-branch-sdk" aria-label="Permalink to &quot;4.1 Initialize the Branch SDK&quot;">​</a></h4><p>Whenever the app starts, it is necessary to initialize the Branch SDK by calling <code>openSession</code> on the <code>Branch</code> instance returned by <code>Branch::create</code>, passing any URI received at the command line as the first argument to <code>Branch::create</code>. If no URI is received, pass a blank string. Because initialization involves an API call, it is recommended to initialize the Branch SDK at the earliest opportunity from <code>wWinMain</code>.</p><p><strong>Create a callback object</strong></p><p>All asynchronous Branch SDK methods require a callback object implementing the <code>BranchIO::IRequestCallback</code> interface. This interface has three methods: <code>onSuccess</code>, <code>onStatus</code> and <code>onError</code>. Either <code>onSuccess</code> or <code>onError</code> is guaranteed to be called exactly once.</p><ul><li>The <code>onStatus</code> method may or may not be called one or more times before <code>onSuccess</code> or <code>onError</code>.</li><li>Link data and other results are returned to the caller in the second argument to <code>onSuccess</code>, a <a href="https://github.com/BranchMetrics/cpp-branch-deep-linking-attribution/blob/master/BranchSDK/src/BranchIO/JSONObject.h" target="_blank" rel="noreferrer">BranchIO::JSONObject</a>.</li><li>The caller manages the lifetime of the request callback. It&#39;s only necessary to guarantee that the callback object live until <code>onSuccess</code> or <code>onError</code> is called. In the examples here and in the repo, the callbacks delete themselves at the end of each of these methods.</li></ul><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;BranchIO/Branch.h&gt;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        using namespace BranchIO;</span></span>
<span class="line"><span>        using namespace std;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        struct MyCallback: public virtual BranchIO::IRequestCallback {</span></span>
<span class="line"><span>        void onSuccess(int id, JSONObject payload) {</span></span>
<span class="line"><span>        // onSuccess or onError guaranteed to be called exactly once</span></span>
<span class="line"><span>        done();</span></span>
<span class="line"><span>        }</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        void onStatus(int id, int code, string message) {</span></span>
<span class="line"><span>        // onStatus may be called one or more times before onStatus or onError</span></span>
<span class="line"><span>        }</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        void onError(int id, int code, string message) {</span></span>
<span class="line"><span>        // onSuccess or onError guaranteed to be called exactly once</span></span>
<span class="line"><span>        done();</span></span>
<span class="line"><span>        }</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        private:</span></span>
<span class="line"><span>        void done() {</span></span>
<span class="line"><span>        delete this;</span></span>
<span class="line"><span>        }</span></span>
<span class="line"><span>        };</span></span></code></pre></div><p><strong>Create a Branch instance and initialize a Branch session</strong></p><p>Create an instance of a callback object, as above, for use with the <code>Branch::openSession</code> method. This callback will receive any link data when the app is opened from a link.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;BranchIO/Branch.h&gt;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        using namespace BranchIO;</span></span>
<span class="line"><span>        using namespace std;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        struct MyCallback : public virtual IRequestCallback;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        int APIENTRY wWinMain(_In_ HINSTANCE hInstance,</span></span>
<span class="line"><span>        _In_opt_ HINSTANCE hPrevInstance,</span></span>
<span class="line"><span>        _In_ LPWSTR lpCmdLine,</span></span>
<span class="line"><span>        _In_ int nCmdShow)</span></span>
<span class="line"><span>        {</span></span>
<span class="line"><span>        UNREFERENCED_PARAMETER(hPrevInstance);</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        // ----- Initialize the Branch SDK</span></span>
<span class="line"><span>        AppInfo appInfo;</span></span>
<span class="line"><span>        appInfo.setAppVersion(&quot;1.0&quot;);</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        auto* branch = Branch::create(branchKey, &amp;appInfo);</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        wstring cmdLineArg(lpCmdLine ? lpCmdLine : L&quot;&quot;);</span></span>
<span class="line"><span>        wstring uriScheme(L&quot;myurischeme&quot;);</span></span>
<span class="line"><span>        if (cmdLineArg[0] == &#39;&quot;&#39;) {</span></span>
<span class="line"><span>        // Strip off any leading and trailing quotes</span></span>
<span class="line"><span>        cmdLineArg = cmdLineArg.substr(1, cmdLineArg.length() - 2);</span></span>
<span class="line"><span>        }</span></span>
<span class="line"><span>        wstring::size_type prefixLength = min(uriScheme.length(), cmdLineArg.length());</span></span>
<span class="line"><span>        wstring prefix = cmdLineArg.substr(0, prefixLength);</span></span>
<span class="line"><span>        if (prefix == uriScheme)</span></span>
<span class="line"><span>        {</span></span>
<span class="line"><span>        // Open any URI passed at the command line</span></span>
<span class="line"><span>        uriToOpen = cmdLineArg;</span></span>
<span class="line"><span>        }</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        auto* myCallback = new MyCallback;</span></span>
<span class="line"><span>        branch-&gt;openSession(uriToOpen, myCallback);</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        // ----- Continue with normal Win32 startup</span></span>
<span class="line"><span>        MyRegisterClass(hInstance);</span></span></code></pre></div><h4 id="_4-2-receive-the-uri-from-the-command-line" tabindex="-1">4.2 Receive the URI from the Command line <a class="header-anchor" href="#_4-2-receive-the-uri-from-the-command-line" aria-label="Permalink to &quot;4.2 Receive the URI from the Command line&quot;">​</a></h4><div class="info custom-block"><p class="custom-block-title">Note</p><p>This entire process is very similar to associating a file extension with an app via the Registry.</p></div><p>The Registry association via <code>shell\\open\\command</code> passes the URI to the app as a command-line argument. This is passed to the app as <code>lpCmdLine</code>, the third parameter in <code>wWinMain</code>.</p><p>The URI received from Windows will have the form <code>myurischeme://open?link_click_id=xxxx</code>, where <code>xxxx</code> is a 64-bit integer. This <code>link_click_id</code> parameter is used to correlate the action of opening the app with the link in the browser.</p><div class="info custom-block"><p class="custom-block-title">Note</p><p>Opening a session with Branch will notify the app of any deferred deep links in the callback.<br> If a URI was passed to <code>openSession</code>, link data will be passed to the <code>onSuccess</code> method in <code>myCallback</code>.</p><p><strong>Important</strong><br> It is necessary to call <code>openSession</code> to initialize the SDK even if a URI is not received.<br> Passing a blank string initializes the SDK and records an open event.</p></div><p><strong>Configuring</strong></p><p>You may configure your app with these classes:</p><table tabindex="0"><thead><tr><th>Class</th><th>Description</th></tr></thead><tbody><tr><td><code>BranchIO::AppInfo</code></td><td>Application Information, such as version and language.</td></tr><tr><td><code>BranchIO::AdvertiserInfo</code></td><td>Advertiser Information, including the ability to disable tracking.</td></tr></tbody></table><p>Any information you provide here is included in events transmitted to Branch.</p><p>Example to disable ad tracking:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>_branchInstance-&gt;getAdvertiserInfo().disableTracking();</span></span></code></pre></div><h4 id="_4-3-handle-multiple-app-instances" tabindex="-1">4.3 Handle Multiple App Instances <a class="header-anchor" href="#_4-3-handle-multiple-app-instances" aria-label="Permalink to &quot;4.3 Handle Multiple App Instances&quot;">​</a></h4><p>Whenever a Branch link is opened, the URI redirect will always result in Windows starting a new instance of the application and passing the URI at launch via the <code>lpCmdLine</code> parameter. This is true not only when opened from a URI, but any time the application is launched from a shortcut, e.g. in the Start menu.</p><p>It is common practice to enforce a single running instance of an application via a shared mutex or similar IPC mechanism. In this case, it is necessary to forward the URI to the running instance. A common approach is to use a <code>WM_COPYDATA</code> message. Pass the URI to the running app in a message before exiting.</p><div class="info custom-block"><p class="custom-block-title">Important Notes</p><ul><li>It is necessary to call <code>openSession</code> from the running instance, not from the ephemeral instance that exits after transmitting the URI.</li><li>It is necessary to call <code>openSession</code> from the running instance even when no URI is received by the ephemeral instance, e.g. when it is launched from the Start menu. A blank string should be forwarded in a <code>WM_COPYDATA</code> message in order that the running instance records an open event. This is necessary for accurate analytics.</li><li>It&#39;s recommended to call <a href="https://docs.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-setforegroundwindow" target="_blank" rel="noreferrer">SetForegroundWindow</a> to bring the running instance to the foreground when a link is opened.</li></ul></div>`,87)])])}const g=a(i,[["render",o]]);export{u as __pageData,g as default};
